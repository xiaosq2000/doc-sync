- id: doc-sync-validate
  name: validate doc-sync configuration
  description: Validate doc-sync.toml and its document targets.
  entry: doc-sync validate
  language: python
  pass_filenames: false
  always_run: true
